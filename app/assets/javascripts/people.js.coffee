# $ ->
#   $('#people')
#     .on 'click' '.compositions .contents', (event) ->
#       toggleViewSwitch $(this).closest('.contents')

# toggleViewSwitch = ($container) ->
#   $container.find('.compositions .contents').toggle()

#   $bodyField  = $container.find('.compositions .contents')
#     if $bodyField.is(':visible')
#       $bodyField
#         .val($container.find('.compositins .contents').text())
#         .select()
                
